<UserControl x:Class="TopCalendar.UI.Modules.Registration.RegistrationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:val="clr-namespace:Bennedik.Validation.Integration.WPF;assembly=Bennedik.Validation.Integration.WPF"
    xmlns:local="clr-namespace:TopCalendar.UI.Modules.Registration"
    x:Name="UserRegistratorControl"
             >
    <val:ErrorProvider x:Name="ValidationErrorProvider">
        <Grid Width="500">

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Right">
                Login
            </Label>
            <TextBox x:Name="LoginTextBox" Grid.Column="1" Grid.Row="0" 
                     HorizontalAlignment="Stretch"                  
                     Text="{Binding  
                        Path=Login, 
                        Mode=OneWayToSource, 
                        UpdateSourceTrigger=PropertyChanged}"                 
            />  
            
            <Label Grid.Column="0" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Right">
                Password
            </Label>
            <PasswordBox x:Name="PasswordTextBox" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch"  
                      local:PasswordHelper.Attach="true"
                      local:PasswordHelper.Password="{Binding 
                        Path=Password, 
                        Mode=OneWayToSource,
                        UpdateSourceTrigger=PropertyChanged}"                      
            />
            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="RegisterButton" Command="{Binding RegisterCommand}" >Register</Button>
                <Button Name="CancelButton" IsCancel="True" Command="{Binding CancelCommand}">Cancel</Button>
            </StackPanel>
        </Grid>
    </val:ErrorProvider>
</UserControl>
